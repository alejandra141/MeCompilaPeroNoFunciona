#include "Basketboller.h"

Basketboller::Basketboller(const std::string& nombre, int velocidad, float posX, float posY, float posZ)
    : nombre(nombre), velocidad(velocidad), posX(posX), posY(posY), posZ(posZ) {
}